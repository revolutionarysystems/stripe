<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xmlns:cm="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.0.0"
           xmlns:camel="http://camel.apache.org/schema/blueprint"
           xsi:schemaLocation="
       http://www.osgi.org/xmlns/blueprint/v1.0.0 http://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd
       http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.0.0 http://aries.apache.org/schemas/blueprint-cm/blueprint-cm-1.0.0.xsd
       http://camel.apache.org/schema/blueprint http://camel.apache.org/schema/blueprint/camel-blueprint.xsd">

    <cm:property-placeholder id="blueprint.placeholder" persistent-id="service_stripe" />
    
    <camelContext xmlns="http://camel.apache.org/schema/blueprint">

        <route>
            <from uri="direct-vm://esb.event.stripe.plans"/>
            <to uri="bean:stripeClient?method=plans()" />
        </route>

        <route>
            <from uri="direct-vm://esb.event.stripe.tokens.getCardToken"/>
            <setBody><simple>${header.formJSON}</simple></setBody>
            <log message="json = ${body}"/>
            <log message="json = ${header.formJSON}"/>
            <to uri="bean:stripeClient?method=getCardToken(${header.formJSON})" />
        </route>

        <route>
            <from uri="direct-vm://esb.event.stripe.customers.create"/>
            <setBody><simple>${header.formJSON}</simple></setBody>
            <log message="json = ${body}"/>
            <to uri="bean:stripeClient?method=customerCreate(${body})" />
        </route>

        <route>
            <from uri="direct-vm://esb.event.stripe.customers.createSubscription"/>
            <setBody><simple>${header.formJSON}</simple></setBody>
            <log message="json = ${body}"/>
            <to uri="bean:stripeClient?method=subscriptionCreate(${body})" />
        </route>

    </camelContext>
    
    <bean id="stripeClient" class="uk.co.revsys.stripe.client.StripeClient">
        <argument value="sk_test_4qo3vbZaV6CI9l3ITyox2St3"/>
    </bean>
    
</blueprint>
